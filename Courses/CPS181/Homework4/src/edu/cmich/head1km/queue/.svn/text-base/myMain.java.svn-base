package edu.cmich.head1km.queue;

public class myMain {

	/**
	 * @author Kyle Head
	 * cps 181 project 4
	 * this program creates an arraylist based queue that accepts Integers and performs basic the basic tasks of a queue.
	 * there is an interface included as well as an exception class that executes whenever an underflow occurs. the main 
	 * class acts as a test for the rest of the program.
	 */
	public static void main(String[] args) {
		Queue <Integer> queue = new Queue <Integer>();
		queue.remove();
		queue.add(5);
		queue.add(7);
		System.out.println("Size:" + queue.size());
		queue.add(11);
		System.out.println("Size:" + queue.size());
		System.out.println("Front:" +queue.front());
		queue.remove();
		System.out.println("Front:" +queue.front());
		
	}

}
